{
  "name": "gestionaseguro",
  "version": "1.0.0",
  "description": "Gestionaseguro.com — El Faro para la regularización 2026",
  "scripts": {
    "dev": "npx tailwindcss -i ./css/tokens.css -o ./css/output.css --watch",
    "build": "npx tailwindcss -i ./css/tokens.css -o ./css/output.css --minify"
  },
  "devDependencies": {
    "tailwindcss": "^3.4.0"
  }
}
